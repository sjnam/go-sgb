% go-sgb: Knuth의 Stanford GraphBase를 한글 GWEB로 옮긴 것.
% 이 파일은 roget_components.w를 Go로 이식한다.
@i ../../gbtypes.w

\input kotexgweb
\def\title{ROGET\_\,COMPONENTS}
\def\<#1>{$\langle${\rm#1}$\rangle$}

@* 강한 성분. 이 시연 프로그램은 Roget의 유의어 사전에서 나온 GraphBase
그래프의 강한 성분(strong component)을, Tarjan 알고리즘의 한 변형[R. E.
Tarjan, ``Depth-first search and linear graph algorithms,'' {\sl SIAM Journal
on Computing\/ \bf1} (1972), 146--160]으로 구한다. 아울러 성분들 사이의 관계도
알아낸다.

두 정점이 서로에게서 유향 경로로 닿을 수 있으면, 그리고 오직 그럴 때만, 같은
강한 성분에 든다. 우리는 강한 성분을 ``역위상 차례(reverse topological
order)''로 찍는다. 곧 |v|가 |u|에서 닿되 |u|는 |v|에서 닿지 못하면, |v|를 품은
성분을 |u|를 품은 성분보다 먼저 적는다. |Roget| 그래프의 정점은 이름과 범주
번호로 함께 나타낸다.

기본 그래프 |roget(1022,0,0,0)|을 돌리면 정점 $1022$개가 강한 성분 $77$개로
갈린다. 그 가운데 하나가 $904$개짜리 거대 성분이고(대표는 범주 $568$
``\.{painting}''이다), 나머지는 정점 하나짜리 $39$개, 둘짜리 $32$개, 셋짜리
$5$개로 자잘하다. 이어서 성분 사이를 잇는 호가 $61$줄 나온다. Roget의 유의어
사전이 얼마나 촘촘히 얽혀 있는지를 보여 주는 셈이다.

@ \UNIX/ 방식 명령줄 옵션으로 여러 그래프를 살펴볼 수 있다. |Roget(n,d,p,s)|의
매개변수를 바꾸려면 \.{-n}\<수>, \.{-d}\<수>, \.{-p}\<수>, \.{-s}\<수>를 쓰고,
그래프 자체를 갈아 끼우려면 \.{-g}\<파일>을 쓴다.

@c
package main

import (
	"fmt"
	"os"
	"strconv"
	@#
	"github.com/sjnam/go-sgb/gbgraph"
	"github.com/sjnam/go-sgb/gbroget"
	"github.com/sjnam/go-sgb/gbsave"
)

// GraphBase 타입을 짧은 이름으로 바로 쓴다.
type (
	Graph  = gbgraph.Graph
	Vertex = gbgraph.Vertex
	Arc    = gbgraph.Arc
)

@<도우미@>

func main() {
	@<명령줄 옵션을 읽는다@>
	@<그래프를 마련한다@>
	fmt.Printf("Reachability analysis of %s\n\n", g.ID)
	@<|g|에 Tarjan 알고리즘을 실행한다@>
}

@ 정점의 |cat_no|(범주 번호)는 |gbroget|이 |U.I|에 담아 두었다. 정점 하나를
``\<번호> \<이름>'' 꼴로 찍는 |specs|를 마련한다. 외부 그래프를 \.{-g}로
복원한 경우엔 범주 번호가 없으므로 정점의 자리 번호(1부터)를 쓴다.

@<도우미@>=
// |specs|는 정점 |v|를 ``번호 이름'' 꼴 문자열로 만든다.
func specs(g *Graph, v *Vertex, fromFile bool) string {
	num := v.U.I // |cat_no|
	if fromFile {
		num = g.Index(v) + 1
	}
	return fmt.Sprintf("%d %s", num, v.Name)
}

@ 각 인자의 접두어를 떼어 해당 매개변수에 넣는다. \.{-g}$\langle$파일$\rangle$은
|roget| 대신 복원할 외부 그래프를 가리킨다.

@<명령줄 옵션을 읽는다@>=
var n, d, p, s int64
var fileName string
var dir string
usage := func() {
	fmt.Fprintf(os.Stderr, "Usage: %s [-nN][-dN][-pN][-sN][-DDIR][-gFILE]\n", os.Args[0])
	os.Exit(2)
}
num := func(str string) int64 {
	x, err := strconv.ParseInt(str, 10, 64)
	if err != nil {
		usage()
	}
	return x
}
for _, arg := range os.Args[1:] {
	switch {
	case len(arg) >= 2 && arg[0] == '-' && arg[1] == 'n':
		n = num(arg[2:])
	case len(arg) >= 2 && arg[0] == '-' && arg[1] == 'd':
		d = num(arg[2:])
	case len(arg) >= 2 && arg[0] == '-' && arg[1] == 'p':
		p = num(arg[2:])
	case len(arg) >= 2 && arg[0] == '-' && arg[1] == 's':
		s = num(arg[2:])
	case len(arg) >= 2 && arg[0] == '-' && arg[1] == 'D':
		dir = arg[2:]
	case len(arg) >= 2 && arg[0] == '-' && arg[1] == 'g':
		fileName = arg[2:]
	default:
		usage()
	}
}

@ 그래프는 |roget|이 짓거나, \.{-g}가 주어졌으면 파일에서 복원한다. 데이터
디렉터리는 \.{-D}로 정하며 기본값은 |"data"|다.

@<그래프를 마련한다@>=
fromFile := fileName != ""
var g *Graph
var err error
if fromFile {
	g, err = gbsave.RestoreGraph(fileName)
} else {
	g, err = gbroget.Roget(n, d, p, s, dir)
}
if err != nil {
	fmt.Fprintf(os.Stderr, "그래프를 만들 수 없습니다 (오류 %v)\n", err)
	os.Exit(1)
}

@* Tarjan 알고리즘. 이 알고리즘은 본디 재귀적이다. 그러나 깊이 겹친 재귀 앞에서
굼떠지는 시스템도 있으므로, 우리는 \GO/의 실행 시간 스택에 기대는 대신 연결
리스트로 재귀를 겉으로 드러내 편다.

각 정점은 알고리즘이 도는 동안 세 단계를 거친다. 처음엔 ``안 본''(unseen)
상태고, 다음엔 ``활성''(active)이 되며, 끝으로 어느 강한 성분에 배정되면
``정착''(settled)한다.

알고리즘의 현재 상태를 나타내는 자료 구조는 정점마다 유틸리티 필드 다섯 개를
써서 만든다: |rank|(|Z.I|), |parent|(|Y.V|), |untagged|(|X.A|), |link|(|W.V|),
|min|(|V.V|). 하나씩 차례로 보자.

@ 첫째는 정수 |rank| 필드로, 안 본 정점에서는 $0$이다. 정점을 처음 살펴보는
순간 그 정점은 활성이 되고 |rank|는 $0$이 아닌 값이 되어 그대로 남는다. 정확히
말하면, $k$번째로 활성이 되는 정점이 rank~$k$를 받는다. 그리고 정점이 마침내
정착하면 그 rank는 무한대로 다시 놓인다.

Tarjan 알고리즘은 동굴의 방을 모두 탐험하려는 간단한 모험 게임이라 생각하면
편하다. 다만 이번에는 방과 방 사이의 통로가 {\sl 한쪽으로만\/} 오갈 수 있다.
어떤 방에 처음 들어가면 그 방에 새 번호를 매기는데, 그것이 rank다. 나중에 같은
방에 다시 들어오게 되면 rank가 $0$이 아닌 것을 보고 ``여긴 벌써 와 봤군''
하면서 얼른 빠져나올 수 있다. (물리쳐야 할 용 따위, 컴퓨터 게임에 딸린 복잡한
것들은 나오지 않는다.)

@ 활성 정점들은 늘 방향 있는 트리를 이루며, 그 호는 원래 그래프의 호 가운데
일부다. |u|에서 |v|로 가는 트리 호는 |v.parent=u|로 나타낸다. 활성 정점은 모두
부모를 가지는데, 그 부모도 보통 활성 정점이다. 딱 하나 예외가 트리의 뿌리로,
그 |parent|는 |nil|이다.

동굴에 빗대면, 방 |v|의 ``부모''란 |v|에 처음 들어가기 바로 전에 있던 방이다.
부모 포인터를 따라가면 언제든 동굴 밖으로 나올 수 있다.

정점이 정착하는 순간 |parent| 필드의 뜻이 달라진다. 그때부터 |v.parent|는 정점
|v|를 품은 강한 성분의 유일한 대표를 가리킨다. 그러므로 정착한 두 정점이 같은
강한 성분에 드는 것은 |parent|가 같을 때, 그리고 그때뿐이다.

@ 원래의 유향 그래프의 모든 호는 깊이 우선 탐색 동안 빠짐없이 살펴진다. 호를
살펴볼 때마다 우리는 그것에 태그를 붙여, 두 번 살피지 않게 한다. 동굴이라면
방과 방 사이의 통로를 한번 지나가 보고 나서 표시해 두는 셈이다.

알고리즘이 |Arc| 레코드에 정말로 태그를 붙이지는 않는다. 그 대신 정점 |v|마다
포인터 |v.untagged|를 두어, |v|에서 나가는 아직 탐험하지 않은 호들을 가리키게
한다. 리스트에서 |v.arcs|와 |v.untagged| 사이에 놓인 호들이 이미 살펴본
것들이다.

@ 알고리즘은 특별한 스택 둘을 지닌다. |activeStack|에는 지금 활성인 정점이
모두 들어 있고, |settledStack|에는 지금까지 정착한 정점이 모두 들어 있다.
정점마다 |link| 필드가 있어 제가 놓인 스택에서 바로 아래 정점을 가리키고, 맨
밑이면 |nil|이다. 활성 스택의 정점들은 밑에서 위로 갈수록 rank가 커지는 차례로
늘 놓인다.

@ 마지막이 |min| 필드인데, 이것이 모든 것을 돌아가게 하는 까다로운 대목이다.
정점 |v|가 안 본 상태거나 정착했으면 그 |min| 필드는 아무 뜻이 없다. 그렇지
않으면 |v.min|은 다음 성질을 가진 활성 정점 |u| 가운데 rank가 가장 작은 것을
가리킨다: |u|가 |v| 자신이거나, 아니면 |v|에서 |u|로 가는 성숙한 트리 호 $0$개
이상에 이어 비트리 호 하나로 끝나는 방향 있는 경로가 있다.

트리 호가 무엇이냐고? 성숙한 호는 또 무엇이냐고? 좋은 질문이다. 그래프의 호에
태그를 붙이는 그 순간, 우리는 그것을 트리 호(활성 노드의 트리에서 새 |parent|
링크에 해당하면)이거나 비트리 호(그밖에)로 가른다. 트리 호는 지금 탐험 중인
정점에서 뿌리까지의 경로 위에 더는 놓이지 않게 될 때 {\sl 성숙\/}한다. 정점도
마찬가지로 그 경로 위에 더는 놓이지 않게 될 때 성숙한다고 말한다. 성숙한
정점에서 나가는 호는 모두 태그되어 있다.

앞서 정점은 저마다 처음엔 안 본 상태, 다음엔 활성, 끝으로 정착이라고 했다.
새 정의를 얹으면 호도 마찬가지임을 알 수 있다. 호는 태그 안 된 상태로 시작해서
비트리 호가 되거나 트리 호가 된다. 뒤엣것이면 처음엔 덜 성숙한 트리 호였다가
끝내는 성숙한다.

지금은 이 정의들을 그냥 믿어 두자. 곧 다 환해질 것이다.

@ 깊이 우선 탐색은 정점을 하나하나 찾아가 그것이 어디로 이어지는지 보면서
그래프를 훑는다. 말했듯 Tarjan 알고리즘에서 활성 정점들은 방향 있는 트리를
이루며, 그 가운데 하나를 현재 정점이라 부른다.

현재 정점에 아직 태그 안 된 호가 남아 있으면 그런 호 하나에 태그를 붙이는데,
여기서 두 경우가 갈린다. 그 호가 안 본 정점으로 이끄느냐 아니냐다. 그렇다면
그 호는 트리 호가 되고, 여태 안 본 정점이던 그것이 활성이 되면서 새 현재
정점이 된다. 그렇지 않고 이미 본 정점으로 이끈다면 그 호는 비트리 호가 되고
현재 정점은 그대로다.

@ 마침내 현재 정점 |v|에 태그 안 된 호가 하나도 남지 않는 때가 온다. 이때
알고리즘은 |v|와 그 자손 모두가 하나의 강한 성분을 이룬다고 판단할 수 있다.
정말로 그 조건은 |v.min==v|일 때, 그리고 그때만 참이다(증명은 아래에 있다).
그렇다면 |v|와 그 자손 모두가 정착해 트리를 떠난다. 그렇지 않으면 |v|의 부모
|u|에서 |v|로 가는 트리 호가 이제 성숙한 것이므로, |v.min|의 값으로 |u.min|의
값을 고친다. 어느 쪽이든 |v|는 성숙하고 |v|의 부모가 새 현재 정점이 된다.
|u|에서 |v|로 가는 호가 성숙할 때 고쳐야 하는 것은 |u.min| 하나뿐임을
눈여겨보라. 다른 어떤 |w.min|도 그대로인데, 갓 성숙한 호에는 성숙한 선행자가
없기 때문이다.

동굴 비유가 상황을 밝혀 준다. |v|에서 시작하는 곁동굴을 |v| 자신을 거쳐
되돌아오지 않고는 빠져나올 수 없고, 게다가 |v|의 모든 자손에서 |v|로 되돌아올
수 있다면, 방 |v|와 그 자손들은 하나의 강한 성분을 이룬다. 그런 강한 성분을
하나 알아내면 우리는 그것을 닫아 걸고 그 곁동굴은 더 탐험하지 않는다.

@ |v|가 트리의 뿌리이면 늘 |v.min==v|이므로, 성숙하는 순간 언제나 새 강한
성분을 정한다. 그러면 |v|에게 부모가 없으니 깊이 우선 탐색이 끝난다. 그러나
Tarjan 알고리즘은 굴하지 않고 아직 안 본 정점 |u|를 찾아 나선다. 그런 정점이
있으면 |u|를 뿌리로 삼아 새 깊이 우선 탐색이 시작된다. 이 과정은 마침내 모든
정점이 행복하게 정착할 때까지 이어진다.

이 알고리즘의 아름다움은, 다음처럼 짜기만 하면 이 모든 것이 아주 효율적으로
돌아간다는 데 있다.

@ 이 규약대로 자료구조를 시작하기는 쉽다. |untagged|를 |arcs|로, |rank|를 0으로
두고, 두 스택을 비우고, 본 정점 수 |nn|을 0으로 둔다.

@<|g|에 Tarjan 알고리즘을 실행한다@>=
var activeStack, settledStack *Vertex
var nn int64
for v := range g.AllVertices() {
	v.Z.I = 0      // |rank|
	v.X.A = v.Arcs // |untagged|
}
@<안 본 정점마다 그를 뿌리로 깊이 우선 탐색을 한다@>
@<성분 사이를 잇는 호를 대표 하나씩 찍는다@>

@ 안 본 정점이 남아 있으면 그를 뿌리로 새 깊이 우선 탐색을 시작한다. 탐색은
연결 리스트로 펼친 되재귀 고리로 돈다: 현재 정점 |v|에서 한 걸음씩 나아가며,
때로 자식으로 내려가고 때로 부모로 되올라간다.

@<안 본 정점마다 그를 뿌리로 깊이 우선 탐색을 한다@>=
for vv := range g.AllVertices() {
	if vv.Z.I != 0 { // 이미 보았다
		continue
	}
	v := vv
	v.Y.V = nil // |parent|
	@<정점 |v|를 활성으로 만든다@>
	for v != nil {
		@<현재 정점 |v|에서 한 걸음 나아간다@>
	}
}

@ @<정점 |v|를 활성으로 만든다@>=
nn++
v.Z.I = nn         // |rank|
v.W.V = activeStack // |link|
activeStack = v
v.V.V = v // |min|

@ 이제 재미있어진다. 하지만 우리가 하는 일은 잘 훈련된 동굴 탐험가라면 침착하게
동굴을 살피며 할 법한 바로 그것일 뿐이다. 현재 정점이 제자리에 머무느냐, 새
자식으로 내려가느냐, 부모로 되짚어 올라가느냐에 따라 크게 세 경우가 있다.
|v|에 아직 태그 안 붙은 호 |a|가 있으면 그것을 태그하고, 그 호가 안 본 정점으로
가면 트리 호가 되어 그리로 내려간다. 이미 본 정점으로 가면 비트리 호이므로
|v.min|만 갱신한다. 남은 호가 없으면 |v|가 성숙하여 부모로 되올라간다.

@<현재 정점 |v|에서 한 걸음 나아간다@>=
if a := v.X.A; a != nil { // |untagged|
	u := a.Tip
	v.X.A = a.Next // |v|에서 |u|로 가는 호를 태그한다
	if u.Z.I != 0 { // |u|를 이미 보았다
		if u.Z.I < v.V.V.Z.I {
			v.V.V = u // 비나무 호이니 |v.min|만 고친다
		}
	} else { // |u|는 아직 안 본 정점이다
		u.Y.V = v // |v|에서 |u|로 가는 호가 새 나무 호가 된다
		v = u     // 이제 |u|가 현재 정점이다
		@<정점 |v|를 활성으로 만든다@>
	}
} else { // |v|의 모든 호가 태그되어 |v|가 성숙한다
	u := v.Y.V // 나무를 되올라갈 채비
	if v.V.V == v {
		@<|v|와 활성 스택 위의 그 자손들을 나무에서 떼어 강한 성분으로 삼는다@>
	} else if v.V.V.Z.I < u.V.V.Z.I {
		u.V.V = v.V.V // |u|에서 |v|로 가는 호가 성숙해 |v.min|이 |u|에 보인다
	}
	v = u // |v|의 옛 부모가 새 현재 정점이 된다
}

@ 활성 스택의 원소들은 늘 rank 순으로 놓이고, 트리에서 정점 |v|의 자식들은
모두 |v|보다 rank가 크다. Tarjan 알고리즘은 그 역에 해당하는 성질에 기댄다:
{\sl 현재 정점 |v|보다 rank가 큰 활성 노드는 모두 |v|의 자손이다.\/} 알고리즘이
전위 순서(preorder), 곧 ``왕위 계승 순서''로 rank를 매기며 트리를 지었기 때문에
이 성질이 성립한다. 맏이와 그 자손이 먼저 오고, 그다음이 둘째, 이런 식이다.
따라서 현재 정점의 자손들은 언제나 스택 꼭대기에 잇달아 나타난다.

@ Tarjan 알고리즘의 또 다른 근본 성질은 좀 더 미묘하다: {\sl 어느 활성
정점에서나 현재 정점으로 가는 길이 늘 있다.\/} 이는 성숙한 활성 정점 |u|가
모두 |u.min.rank<u.rank|를 만족한다는 사실에서 따라 나온다. 어떤 활성 정점이
현재 정점 |v|로 이끌지 못한다고 하고, 그런 반례 가운데 rank가 가장 작은 것을
|u|라 하자. 그러면 |u|는 |v|의 조상이 아니므로 성숙했을 수밖에 없다. 따라서
|u|는 활성 정점 |u.min|으로 이끄는데, |u.min|은 rank가 더 작으니 반례가 아니라
|v|로 가는 경로가 {\sl 있다.\/} 가정에 어긋난다.

@ 그러므로 |v|와 그 활성 자손들은 모두 서로에게서 닿을 수 있고, 같은 강한
성분에 들어야 한다. 게다가 |v.min==v|이면 이 성분을 더 키울 수 없다. 이
정점들에서 안 본 정점으로 가는 호가 없기 때문이다---|v|와 그 자손에서 나가는
호는 이미 모두 태그되었다. 또 이 정점들에서 스택상 |v|보다 아래에 있는 활성
정점으로 가는 호도 없다. 있었다면 |v.min|의 rank가 |v|보다 작았을 것이다.
따라서 이 정점들에서 다른 정점으로 가는 호가 있다면 그것은 모두 정착한 정점으로
간다. 그리고 앞선 계산 단계에서 우리는 정착한 정점들이 모두 다른 강한 성분에
든다는 것을 알고 있다.

그러므로 |v|와 그 활성 자손들을 지금 정착시키는 것이 옳다. 그들을 활성 정점의
트리에서 떼어 내더라도, |v|의 rank보다 작은 rank를 가진 정점으로 가는 경로가
있는 정점이 함께 떨어져 나가지는 않는다. 따라서 그들을 떼어 내는 일은 아직
활성으로 남은 어떤 정점 |u|의 |u.min| 값의 정당성도 해치지 않는다.

읽는 이가 주장된 성분이 참으로 강함을 손쉽게 확인할 수 있을 만큼은 정보를
찍어 준다. 활성 스택 맨 위에 잇달아 놓인 그들을 정착 스택으로 옮기는 것은
한 스택의 꼭대기 토막을 다른 스택에 얹는 일이라 이음 몇 개만 고치면 된다.
무한대 rank로는 |g.N|을 쓴다(넉넉히 크다).

@<|v|와 활성 스택 위의 그 자손들을 나무에서 떼어 강한 성분으로 삼는다@>=
t := activeStack
activeStack = v.W.V
v.W.V = settledStack
settledStack = t // 한 스택의 꼭대기를 다른 스택으로 옮겼다
fmt.Printf("Strong component `%s'", specs(g, v, fromFile))
if t == v {
	fmt.Println() // 정점 하나짜리
} else {
	fmt.Println(" also includes:")
	for t != v {
		fmt.Printf(" %s (from %s; ..to %s)\n",
			specs(g, t, fromFile), specs(g, t.Y.V, fromFile), specs(g, t.V.V, fromFile))
		t.Z.I = g.N // 이제 |t|는 정착했다
		t.Y.V = v   // 그리고 |v|가 새 강한 성분을 대표한다
		t = t.W.V
	}
}
v.Z.I = g.N // |v|도 정착했다
v.Y.V = v   // 그리고 제 강한 성분을 대표한다

@ 강한 성분을 다 찾은 뒤엔 성분 사이의 관계도, 어떤 이음도 두 번 말하는 일
없이 셈할 수 있다. 우리가 |settledStack|을 쌓아 둔 것이 바로 이 일을 정렬도
검색도 없이 해내기 위해서였다. 강한 성분 그 자체에만 관심이 있다면 이 단계는
없어도 된다.

이 단계에서 원본은 아까 |untagged|로 쓰던 필드 |x|를 |arc_from|이라는 이름으로
다시 쓴다. \CEE/의 공용체 덕에 같은 자리를 |Arc*|로도 |Vertex*|로도 볼 수
있었기 때문이다. \GO/에서는 |X.A|와 |X.V|가 애초에 서로 다른 필드라 그냥 따로
쓰면 된다.

@ 요령은 {\sl 같은 강한 성분의 정점들이 |settledStack|에 나란히 모여 있다\/}는
데 기댄다. 스택을 밑에서 위로 훑으며, 정점 |v|를 볼 때마다 그 성분의 대표 |u|를
꺼내 |u.arcFrom|에 |u| 자신을 적어 둔다. 그러면 |u|를 대표로 하는 성분을 훑는
동안 내내 그 표시가 남아 있다. 이제 |v|에서 나가는 호가 대표 |w|인 성분으로
가는데 |w.arcFrom|이 아직 |u|가 아니면, 그것은 성분 |u|에서 성분 |w|로 가는
이음을 처음 본 것이다. 찍고 나서 |w.arcFrom|에 |u|를 적어 두면 같은 이음을
다시 찍지 않는다. 성분이 바뀌면 |u|도 바뀌므로 표시는 저절로 무효가 된다---따로
지울 것이 없다.

@<성분 사이를 잇는 호를 대표 하나씩 찍는다@>=
fmt.Println("\nLinks between components:")
for v := settledStack; v != nil; v = v.W.V {
	u := v.Y.V // |parent|, 곧 |v|의 성분 대표
	u.X.V = u  // |arcFrom|
	for a := range v.AllArcs() {
		w := a.Tip.Y.V // |a.Tip|의 성분 대표
		if w.X.V != u {
			w.X.V = u
			fmt.Printf("%s -> %s (e.g., %s -> %s)\n",
				specs(g, u, fromFile), specs(g, w, fromFile),
				specs(g, v, fromFile), specs(g, a.Tip, fromFile))
		}
	}
}

@* 찾아보기.
